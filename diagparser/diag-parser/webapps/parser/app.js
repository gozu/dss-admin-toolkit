// The application initializes itself in body.html
// No configuration parameters are required as all processing is done client-side
// and the interface is self-contained in the HTML file

document.addEventListener('DOMContentLoaded', function() {
    // Console logging for admin debugging
    console.log("Diag Parser webapp initialized");
    // The actual initialization happens in the body.html file
});