# Handoff (2026-02-05)

## Current state
- Live webapp backend restart succeeded and `GET /__ping` now returns `200` (ping loop fixed).
- Plugin deployed multiple times up to `1.2.5` (`git` HEAD: `0ac19b3`).
- Remote visual comparison against locked legacy baseline is mostly aligned.
- Latest compare label `after2`:
  - matched: `projects-table`, `clusters-table`, `log-errors-section`, `filesystem-table`, `userStats-table`, `systemLimits-table`
  - still missing: `licenseProperties-table`

## Runtime endpoint status (live backend)
- `GET /api/mode`: `200`
- `GET /api/clusters`: `200` with cluster data
- `GET /api/logs/errors`: `200` with populated extracted errors
- `GET /api/license`: `200` but empty payload (`licenseProperties: {}`)

## Pending local (not yet deployed)
- Added license fallback in `webapps/diag-parser-live/backend.py` so `licenseProperties` is always non-empty and section should render even when license source is unavailable.
- Added DSS API wrapper script: `scripts/dss_api.sh`
- Updated compare harness: `resource/frontend/remote-compare.mjs`
- New compare artifacts for `after2`

## Git working tree snapshot at handoff
- Modified:
  - `resource/frontend/package-lock.json`
  - `resource/frontend/remote-compare.mjs`
  - `resource/frontend/screenshots/remote-compare/current/live-after-full.png`
  - `resource/frontend/screenshots/remote-compare/current/live-after-summary.json`
  - `resource/frontend/screenshots/remote-compare/current/live-after-top.png`
  - `resource/frontend/screenshots/remote-compare/reports/compare-after.json`
  - `webapps/diag-parser-live/backend.py`
- Untracked:
  - `scripts/dss_api.sh`
  - `resource/frontend/screenshots/remote-compare/current/live-after2-full.png`
  - `resource/frontend/screenshots/remote-compare/current/live-after2-summary.json`
  - `resource/frontend/screenshots/remote-compare/current/live-after2-top.png`
  - `resource/frontend/screenshots/remote-compare/reports/compare-after2.json`

## Resume commands
1. Deploy pending local changes:
```bash
python3 -m py_compile webapps/diag-parser-live/backend.py
make deploy
```

2. Restart live backend:
```bash
bash scripts/dss_api.sh PUT /public/api/projects/PYTHONAUDIT_TEST/webapps/haoMNtw/backend/actions/restart --base-url http://localhost:10000
```

3. Validate backend state/log tail:
```bash
bash scripts/dss_api.sh GET /public/api/projects/PYTHONAUDIT_TEST/webapps/haoMNtw/backend/state --base-url http://localhost:10000 --raw | jq -r '.currentLogTail.lines[]'
```

4. Run compare again:
```bash
cd resource/frontend
node remote-compare.mjs --label=after3
```

5. Check final diff:
- `resource/frontend/screenshots/remote-compare/reports/compare-after3.json`

## Baseline files (read-only, reused)
- `resource/frontend/screenshots/remote-compare/baseline/legacy-baseline-full.png`
- `resource/frontend/screenshots/remote-compare/baseline/legacy-baseline-top.png`
- `resource/frontend/screenshots/remote-compare/baseline/legacy-baseline-summary.json`

## Update (20:53 UTC)
- Deployed successfully via `make deploy` at plugin version `1.2.6` (commit `9927d37`).
- Restarted webapp backend `PYTHONAUDIT_TEST.haoMNtw`.
- Confirmed backend health:
  - `GET /__ping` -> `200`
  - `GET /api/overview` -> `200` (no `dict.split` crash)
  - `GET /api/license` -> `200` with fallback `licenseProperties` (3 fields)
- New compare run `after3` now fully matches legacy for tracked sections, including `licenseProperties-table`.
- New artifacts:
  - `resource/frontend/screenshots/remote-compare/current/live-after3-full.png`
  - `resource/frontend/screenshots/remote-compare/current/live-after3-top.png`
  - `resource/frontend/screenshots/remote-compare/current/live-after3-summary.json`
  - `resource/frontend/screenshots/remote-compare/reports/compare-after3.json`

## Wrapper script note
- `scripts/dss_api.sh` now supports:
  - `--form` for multipart upload
  - `--header` for extra headers
  - localhost rewrite: when base URL is `http://localhost:10000` and path starts with `/public/api/...`, it auto-translates to `/dip/publicapi/...`.

## Update (21:08 UTC)
- Deployed `v1.2.7` (commit `b2a01d4`) with datadir footprint API drilldown fixes.
- `/api/dir-tree` now supports scope params and fuller API coverage:
  - `scope=all` (default) -> `compute_all_dss_footprint`
  - `scope=global` -> `compute_global_only_footprint`
  - `scope=unknown` (+ `summaryOnly=true|false`) -> `compute_unknown_footprint`
  - `scope=project&projectKey=<KEY>` -> `compute_project_footprint`
- Footprint parsing now supports DSS 14 payload shape (`items`, nested dicts, future `result` wrappers), so root children are populated.
- Verified API responses:
  - `/api/dir-tree?maxDepth=2` returns 23 root children (was empty previously)
  - `/api/dir-tree?scope=global` returns global subtree
  - `/api/dir-tree?scope=project&projectKey=PYTHONAUDIT_TEST` returns project subtree
- Treemap drilldown click verified via Playwright: breadcrumb changes from `dss_data` to `dss_data / codeEnvs`.
- New comparison artifacts:
  - `resource/frontend/screenshots/remote-compare/current/live-after4-full.png`
  - `resource/frontend/screenshots/remote-compare/current/live-after4-top.png`
  - `resource/frontend/screenshots/remote-compare/current/live-after4-summary.json`
  - `resource/frontend/screenshots/remote-compare/reports/compare-after4.json`
